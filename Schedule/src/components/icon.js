import React from 'react';

const Icon = (icon) => {
    return (
        <div className="icon">
        <i className={`${icon} ${className}`}></i>
        

        </div>
    )
}

export default Icon;
